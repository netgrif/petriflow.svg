describe('Petri Nets SVG tests', () => {

    beforeEach(() => {
        console.log("Before each test this will be run")
    });

    // @ts-ignore
    test('should start', () => {
        expect(1).toEqual(1);
    });

});
